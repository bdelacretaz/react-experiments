# netlify HTTP headers config
# https://docs.netlify.com/routing/headers/#syntax-for-the-headers-file

# keep images for an hour
/images/personnes/*
  cache-control: public, max-age=3600, must-revalidate
